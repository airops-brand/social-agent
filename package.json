{
  "name": "nuggets-agent",
  "version": "1.0.0",
  "description": "Slack → Claude → Notion → Slack approval loop for AirOps post ideas",
  "main": "agent.js",
  "scripts": {
    "start": "node agent.js",
    "dev": "node --watch agent.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.20.0",
    "@notionhq/client": "^2.2.15",
    "@slack/bolt": "^3.18.0",
    "dotenv": "^16.4.5"
  }
}
